// WAP to calculate the arithmetic operator
/*
    + -> Addition
    - -> Subtraction
    * -> Multiplication
    / -> Division
    % -> Modulo (return the reminder)
    ++ -> Increment by 1
        for ex. no = 5;
            ->no++ ->no = no + 1 -> no = 6; 
            ->++no ->no = no + 1 -> no = 6; 
    -- -> Decrement by 1
            for ex. no = 5;
                ->no-- ->no = no - 1 -> no = 4; 
                ->--no ->no = no - 1 -> no = 4; 

*/

#include<stdio.h>
int main()
{
    int a, b;

    printf("Enter the value of a:");
    return 0;
}
